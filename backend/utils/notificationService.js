const sendNotification = (method, message) => {
    // Logica per inviare notifiche in base al metodo specificato
    if (method === 'system') {
      // Codice per inviare una notifica di sistema
    } else if (method === 'email') {
      // Codice per inviare un'email
    } else if (method === 'alert') {
      // Codice per mostrare un alert
    }
  };
  
  module.exports = { sendNotification };
  